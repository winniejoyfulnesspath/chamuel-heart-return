# Chamuel Heart Return — 大天使夏彌爾・心之歸位

Winnie Lim 8月15日免費公開冥想活動頁。

靜態頁（`index.html` + `favicon.png`），連住 Netlify 自動部署：
`git push` 去 `master` 即自動 deploy 去 `chamuel-heart-return.netlify.app`。

改嘢直接編輯 `index.html`，commit + push 就得，唔使再手動拖 zip 上 Netlify Drop。
